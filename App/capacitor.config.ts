import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'atmosferas',
  webDir: 'www',
  server: {
    androidScheme: 'https',
    hostname: 'nelsonhernandezg.com'
  }
};

export default config;
